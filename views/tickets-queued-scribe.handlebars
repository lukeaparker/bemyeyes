  
<div class="row">
      <div class="col-sm-2 col-sm-offset-3 bg-card">
    			<nav id="sidebar">
				<div class="p-4 pt-5">
		  		<a href="#" class="img logo rounded-circle mb-5" style="background-image: url(images/logo.jpg);"></a>
<ul class="nav navbar-nav navbar-right">
  {{#if currentUser}}
    <li><a href="/logout">Logout</a></li>
      <a href="/tickets/queued/scribe">Queued</a>
  {{else}}
    <li><a href="/login">Login</a></li>
    <li><a href="/sign-up">Sign Up</a></li>
  {{/if}}
</ul>



	      </div>
    	</nav>

    </div>
    <div class="col-lg bg-card">
        <ul>
            {{#each tickets}}

              <li class="list-group-item list-group-item-card">
                <div class="lead">
                  <a href="/tickets/scribe/{{this._id}}" class="lead">{{this.imageName}}</a>
                </div>
                <div class="text-right">
                  <a href="/n/{{this.profile}}">{{this.author.username}}</a>
                </div>
                </li>
            {{/each}}
        </ul>
    </div>
</div>



